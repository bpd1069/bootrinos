!#/bin/sh
sleep 10
sudo udhcpc
# if on GCE
sudo route add -net 10.240.0.0 netmask 255.255.0.0 dev eth0
sudo route add default gw 10.240.0.1 eth0
route -n
echo Starting ssh
sudo /usr/local/etc/init.d/openssh start
sudo cat /etc/resolv.conf
host www.google.com
sudo cat /etc/resolv.conf
sudo cat /proc/partitions
echo fdisk
sudo fdisk -l
echo blkid
sudo blkid
echo ls /
ls /
# if on GCE
sudo mkdir -p /mnt/sda1
sudo mount /dev/sda1 /mnt/sda1
sudo mkdir -p /mnt/hda1
sudo mount /dev/hda1 /mnt/hda1
# if on EC2
sudo mkdir -p /mnt/xvda1
sudo mount /dev/xvda1 /mnt/xvda1
cd /mnt
sudo find
sudo ls /dev
sudo chown -R tc:staff /opt
sudo chown -R tc:staff /extras
#tce-load -iw python
ifconfig -a
route
ping -c 10 169.254.169.254
ping -c 10 10.240.0.1
cd /extras
sudo cat /etc/resolv.conf
host www.google.com
sleep 30
python3 /extras/beacon.py
#sudo /extras/runkernel
#sleep 10
#sudo reboot
#python3 /extras/beacon.py
echo finished startup

